# worker

Not started yet. This package gets its first code in **Phase 4** (Redis consumer + Judge0 client), once the API + Redis queue (Phase 3) are in place. See the root [README](../README.md) for the phase plan.
